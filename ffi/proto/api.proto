syntax = "proto3";
package api;

import "google/protobuf/timestamp.proto";

message GenericPayload {
    string content = 1;
    map<string, string> metadata = 2;
    bytes payload = 3;
    int64 timestamp = 4; // Menggunakan Unix Timestamp (lebih cepat di-parse)
}